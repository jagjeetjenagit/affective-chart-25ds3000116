Q5 Affective Chart package

Files:
- prompt.txt: final prompt to generate image
- submission.json: required JSON for upload

Next steps:
1) Generate image using prompt.txt (min 1024px short side).
2) Save image as image.png in this folder.
3) Push this folder to a public GitHub repo.
4) Submit two RAW URLs in one line:
   <raw image url> <raw submission.json url>
